import { Card } from "../components/Card";

export function AdminPage() {
  return (
    <div className="page-grid">
      <Card title="Admin Dashboard">
        <p>Admin dashboard shell is ready for Phase 6 content.</p>
        <p>Use admin navigation links to access skills and logs sections.</p>
      </Card>
    </div>
  );
}