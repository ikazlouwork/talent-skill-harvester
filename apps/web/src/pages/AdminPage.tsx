import { Card } from "../components/Card";

export function AdminPage() {
  return (
    <Card title="Admin Dashboard">
      <p>Admin dashboard shell is ready for Phase 6 content.</p>
    </Card>
  );
}