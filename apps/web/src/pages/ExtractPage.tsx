import { Card } from "../components/Card";

export function ExtractPage() {
  return (
    <div className="page-grid">
      <Card title="Extract">
        <p>CV + IFU input form will be implemented in Phase 3.</p>
        <p>This page is ready to host the extraction form and submit flow.</p>
      </Card>
    </div>
  );
}