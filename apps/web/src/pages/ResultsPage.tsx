import { Card } from "../components/Card";

export function ResultsPage() {
  return (
    <div className="page-grid">
      <Card title="Results">
        <p>Structured skills review will be implemented in Phase 3.</p>
        <p>This route is prepared for loading, empty, and error states.</p>
      </Card>
    </div>
  );
}